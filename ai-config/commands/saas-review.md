---
description: Whole-app SaaS design review — enumerate and visit every route, audit against the expensive-UI criteria, report prioritised findings, then apply the approved fixes
---

Load and follow the `saas-review` skill (ai-config/skills/saas-review/SKILL.md, distributed
to `~/.claude/skills/saas-review/`). Run its six phases in order — ground truth, route
inventory, route walk with evidence, audit, findings report with a hard approval stop, then
batched fixes with verification — honouring the project's own quality gate after every batch,
for: $input
