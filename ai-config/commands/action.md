---
description: Combo workflow — spec + plan + build + review, from idea to reviewed code in one command
---

Load and follow the `action` skill (ai-config/skills/action/SKILL.md, distributed
to `~/.agents/skills/action/`). Run its full spec → plan → build → review cycle
for: $input
