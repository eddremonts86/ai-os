---
description: Alias for /saas-review (common "SaaS" typo) — whole-app SaaS design review and remediation
---

Load and follow the `saas-review` skill (ai-config/skills/saas-review/SKILL.md). Mention once
that the canonical command is `/saas-review`. If the request is actually about Sass/SCSS
stylesheets rather than a SaaS product, say so and handle that instead. Scope: $input
