---
description: Complete end-to-end delivery — spec + plan + build + review + code-simplify + ship
---

Load and follow the `full` skill (ai-config/skills/full/SKILL.md, distributed to
`~/.agents/skills/full/`). Run its full spec → plan → build → review → simplify →
ship cycle for: $input
