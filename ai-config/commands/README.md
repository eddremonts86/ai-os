# ai-config/commands

Placeholder for shared slash-commands / CLI commands distributed to the CLIs
(same model as `ai-config/skills/`). Empty on purpose — nothing has needed a
shared command yet. When adding one, mirror the skills flow: put the source
here and wire the symlink in `setup/install-mac.sh`.
