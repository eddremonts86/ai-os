---
description: Generate a rich, responsive, self-contained HTML document from a description or convert a .md/.mdx file
---

Load and follow the `gen-html` skill (ai-config/skills/gen-html/SKILL.md,
distributed to `~/.agents/skills/gen-html/`), including its base template at
`~/.agents/skills/gen-html/assets/base.html`. Generate the document for: $input

Always write the output file to disk (never chat-only output) and confirm the
saved path.
