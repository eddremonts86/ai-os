name: filesystem
description: Read/write files outside the cwd
transport: stdio
command: npx
args:
  - -y
  - '@modelcontextprotocol/server-filesystem'
  - '${HOME}/Projects'
  - '${HOME}/Documents'
enabled: true
