name: filesystem
description: Read/write files outside the cwd
transport: stdio
command: npx
args:
  - -y
  - "@modelcontextprotocol/server-filesystem@2026.7.10"
  - "${HOME}/Projects"
  - "${HOME}/Documents"
enabled: true
