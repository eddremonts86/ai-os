#!/usr/bin/env bash
# npm-globals.txt — global Node packages (installed with pnpm, not npm)
# Install: while read pkg; do pnpm add -g "$pkg"; done < npm-globals.txt

# AI / Claude
@anthropic-ai/claude-code
@openai/codex
@google/gemini-cli

# Hermes (if not installed via pip)
# Edd installs Hermes via pipx or uv, not npm

# Agent-related
# mcp-server-git is Python-only (run via `uvx mcp-server-git`), not on npm
@modelcontextprotocol/server-filesystem
@modelcontextprotocol/server-github
@upstash/context7-mcp
@playwright/mcp

# Build / Dev
typescript
tsx
vite
create-vite
create-next-app
create-nuxt
@nestjs/cli
ts-node
nodemon
concurrently
npm-check-updates
serve
http-server

# Quality
eslint
prettier
@biomejs/biome
knip
depcheck
license-checker

# Git
git-open
git-cz

# Utilities
# npx ships with npm; ncu is the same binary as npm-check-updates (above);
# pnpm and yarn come from brew (Brewfile) — all removed to avoid EEXIST conflicts
npkill
taze
np
bumpp
zx
ts-node-dev
lerna
nx
turbo

# AI dev utilities
# gitingest is Python-only (see pip-packages.txt), not on npm
repomix
llm-cmd
groq-cli
openai-cli
claude-code-router
mcp-cli
# mcp-server-filesystem removed: same binary as @modelcontextprotocol/server-filesystem
