# ~/.warp/ — Warp terminal config (Mac only)
# Warp stores its config in SQLite (~/Library/Group Containers/2BBY89MBSN.dev.warp/)
# This file documents the recommended settings. To apply, run setup/install-mac.sh
# which configures defaults via `defaults write`.

# Theme
theme = dark
window_opacity = 0.95

# Font
font_family = "CaskaydiaCove Nerd Font"
font_size = 14
line_height = 1.4
terminal_font_ligatures = false

# Cursor
cursor_blink = true
cursor_shape = beam

# Behavior
copy_on_select = false
scroll_speed = 3

# AI
ai_features_enabled = true
ai_shortcut = "Cmd+I"

# Keyboard shortcuts (defaults)
# Cmd+T         → New tab
# Cmd+Shift+T   → Reopen closed tab
# Cmd+W         → Close tab
# Cmd+D         → Split pane (horizontal)
# Cmd+Shift+D   → Split pane (vertical)
# Cmd+K         → Clear screen
# Cmd+I         → AI Command Search
# Cmd+Shift+R   → Workflows
# Cmd+Shift+P   → Command Palette
